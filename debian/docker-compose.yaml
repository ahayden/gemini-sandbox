services:
  sandbox:
    build: .
    image: localhost/gemini-sandbox-debian:latest
    pull_policy: never
    container_name: gemini-sandbox-debian
    stdin_open: true
    tty: true
    init: true
    network_mode: "host"
    volumes:
      - ${PWD}:/workspace
