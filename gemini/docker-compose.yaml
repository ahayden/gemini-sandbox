services:
  sandbox:
    build: .
    image: localhost/gemini-sandbox:latest
    pull_policy: never
    container_name: gemini-sandbox
    stdin_open: true
    tty: true
    init: true
    network_mode: "host"
    volumes:
      - ${PWD}:/workspace
